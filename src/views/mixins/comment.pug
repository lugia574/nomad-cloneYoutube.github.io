mixin comment(video, comments)
    ul
        span=comments
        each comment in video.comments.reverse()
            li.video__comment(data-id=comment.id)#video__comment
                div
                    i.fas.fa-user
                    span  #{comment.text}
                if String(comment.owner) === String(loggedInUser._id)
                    button  ✖